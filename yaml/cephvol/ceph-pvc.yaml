apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    name: "wx"
    phone: "123456"
    company: "qingcloud"
    project: "kubesphere"
  name: claim-rox
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fast
  resources:
    requests:
      storage: 1Gi
