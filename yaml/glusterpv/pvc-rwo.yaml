kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: rwo
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
  storageClassName: gluster
